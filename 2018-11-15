有关C语言中的数组，可以通过这样的算法通过输入的方式将数据储存到数组中。
#include <stdio.h>
#define SIZE 5

int main()
{
	int score[SIZE];
	for(int i = 0;i < SIZE;i++){
		printf("Please enter the grades of students: ");
		scanf("%d",&score[i]);
	}

	for(int j = 0;j < SIZE;j++){
	printf("%-3d",score[j]);
	}
    return 0;
}


